extends layout

block content
  h1= title
  - var result = results 
  div.container-fluid
     div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) Diamond_Name: #{item["diamond_name"]}  <br>  Diamond_size: #{item["diamond_size"]} <br> Diamond_type: #{item["diamond_type"]} 
  script.
      function buy(index){
         alert("you purchased item "+(index+1))
      }